{
  "name": "quarkus-migration-agent",
  "version": "1.0.0",
  "description": "Config-driven Spring Boot -> Quarkus migration agent (Node.js harness).",
  "type": "module",
  "bin": { "migrate": "./harness/agentLoop.js" },
  "scripts": {
    "migrate": "node harness/agentLoop.js --config config/migration.yaml",
    "migrate:openrewrite": "node scripts/openrewrite-migrate.js",
    "migrate:openrewrite:apply": "node scripts/openrewrite-migrate.js --apply"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "yaml": "^2.5.0"
  }
}
